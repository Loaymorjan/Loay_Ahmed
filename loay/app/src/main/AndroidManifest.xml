<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.myapplication">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View_Task"></activity>
        <activity android:name=".New_Task" />
        <activity android:name=".NewList" />
        <activity android:name=".empty_result" />
        <activity android:name=".empty_list" />
        <activity android:name=".TaskEmpty" />
        <activity android:name=".result" />
        <activity android:name=".tasks" />
        <activity android:name=".List" />
        <activity android:name=".Login" />
        <activity android:name=".register" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>